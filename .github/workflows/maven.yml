# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 2
    - name: Update Version Number
      env:
        CI_COMMIT_AUTHOR: CICD-Boy
      shell: bash
      run: |
        sed -i "s|^[ \t]*public static final String version.*|\tpublic static final String version = \"$(date '+%b %d, %Y')\";|g" src/main/java/envision/Envision.java
        git add src/main/java/envision/Envision.java
        git diff
        git config --global user.name "${{ env.CI_COMMIT_AUTHOR }}"
        git config --global user.email "CICD-Boy@placeholder.com"
        git commit -m "updating version string : `date +'%b %d, %Y - %H:%M:%S'`"
        git push
    - name: Set up JDK 18
      uses: actions/setup-java@v3
      with:
        java-version: '18'
        distribution: 'temurin'
        cache: maven
    - name: Deploy
      run: mvn -B --batch-mode deploy --file pom.xml
      env:
        MAVEN_USERNAME: ${{ secrets.DEPLOYMENT_USERNAME }}
        MAVEN_PASSWORD: ${{ secrets.DEPLOYMENT_PASSWORD }}
